# ---------------------------------------------------------------
# 依赖
# ---------------------------------------------------------------
node_modules/

# ---------------------------------------------------------------
# 运行时缓存（全部可随时删除，服务会自动重建）
#   ms-playwright/    Chromium 内核，首次用「浏览器桥接」时按需下载
#   browser-profile/  桥接的登录态，含账号会话，绝不入库
#   shots/            自检截图
# ---------------------------------------------------------------
.cache/

# ---------------------------------------------------------------
# 本地 AI 协作记录（开发日志 / 会话记忆，含本机路径，不入库）
# ---------------------------------------------------------------
.workbuddy/

# ---------------------------------------------------------------
# 密钥与本机路径
#   配置文件里 apiKey 一律留空，走环境变量
#   ask.config.local.json 是本机覆盖层：工作区指向这类「只对这台机器成立」
#   的信息写在这里。页面上（首页 / 设置）改工作区就是写这个文件，
#   所以它绝不能进版本库 —— 否则每 clone 一次都会带上别人的 D:\ 路径。
# ---------------------------------------------------------------
ask.config.local.json
# 如果你更习惯把 key 写在本地文件里，也可以放进上面这个文件
# *.key

# ---------------------------------------------------------------
# 系统杂物
# ---------------------------------------------------------------
Thumbs.db
desktop.ini
.DS_Store
*.log
