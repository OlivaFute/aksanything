# 提示词全清单

> 本文件反映**当前实际生效**的提示词（含配置里的和硬编码在服务端的）。
> 随时可自查：
> ```bash
> node tools/ask-server.mjs --dry-run --mode define    # 解释这个词
> node tools/ask-server.mjs --dry-run --mode explain   # 解释这段
> node tools/ask-server.mjs --dry-run --mode qa        # 自由追问
> ```
> 输出会打印完整拼装结果（system + 上下文 + 问题）和字符数。

---

## 一、可配置的三条（`/config` 页可改）

### ① `systemPrompt` — **三种模式共用**，走 system role

```
你是这位学习者的一对一老师。他正在读你为他的学习目标编写的 lesson，现在划出了其中一段，想让你解释、定义或回答。

通用要求：
1. 用中文，直接回应他的要求。不要客套，不要复述他的话。
2. 回答要紧扣他划出的原文，不要泛泛而谈教科书内容。
3. 简洁优先。他是在阅读途中看的，一段话能说清就别写三段；确实需要展开时用短列表。
4. 如果回答本身涉及课内其它章节或术语表，可以直接引用；但不要为了显示关联而强行关联。
5. 需要公式时用 LaTeX（行内 $...$，独立成行 $$...$$）。
6. 不确定的地方直接说明不确定，不要编造数据或文献。

如果下面的【要求】或【他的问题】与以上通用要求冲突，以那个更具体的要求为准。
```

> **措辞必须中性。** 它同时管三种模式，任何偏向某一个模式的表述都会和别的模式打架
> （第 2 条原本是「优先结合所在章节」、第 4 条原本是「主动指出关联位置」，已被中性化）。

### ② `prompts.define` — 「解释这个词」

```
请给我划出的这个词(或短语)下一个准确的定义。

要求：
- 第一句就是定义，且必须是完整的句子（该有主语就有主语，例如「景深是…」）。
- 不要用「这个词是指…」这类铺垫句式，也不要先把这个词单独复述一遍。
- 接着用 1~2 句补充：它为什么重要，或给一个具体例子、数量级。
- 全部不超过 120 字。
- 如果这是本课术语，与课内的用法保持一致。
- 不要用 Markdown 标题，不要客套。
```

### ③ `prompts.explain` — 「解释这段」

```
请深入浅出地解释我划出的这段内容。

要求：
- 重点是把这段话里的概念和道理真正讲明白，让没有基础的人也能听懂。
- 可以用类比、具体数字，或者「换句话说」的方式重新表述一遍。
- 如果有助于理解，可以顺带提到它与课内其它内容的关系，但不强求。
- 需要公式时用 LaTeX（行内 $...$）。
- 不要客套，不要复述原文。
```

---

## 二、硬编码的上下文模板（配置页看不到）

在 `ask-server.mjs` 的 `buildMessages()` 里，上面那条「问题」之前会拼上一段上下文。
**分隔符全部是硬编码的**，改这些要动代码：

```
【学习者的目标（来自 MISSION.md）】
{ MISSION 的「为什么学/最终目标/成功标准/约束」几节 }

【课程目录】
- 0001-three-algorithms-overview.html　三种算法，一个共同点

【他正在读的课：三种算法，一个共同点（lessons/0001-...html）】
本课章节：一、为什么必须有 Z 扫描 / 二、差别只在纵轴 / …

【本课全文】                      ← define 模式下这里是【所在章节全文】
{ 整课正文，约 3000 字 }

【他划出的原文】                  ← 多段引用时为【他引用的原文（共 N 段）】+ 编号列表
> {选中的那段}

【该段完整上下文】
{ 所在段落全文 }

【当前阅读位置】章节「一、为什么必须有 Z 扫描」

【他的问题】                      ← define 模式下这里写作【要求】
{ 上面 ② / ③ 的内容，或用户自己敲的问题 }
```

上下文范围由两项控制：

| 配置项 | 作用 | 默认 |
|---|---|---|
| `context.lessonMode` | explain / qa 的正文范围 | `full`（整课全文） |
| `context.defineLessonMode` | **define 单独的正文范围** | `section`（仅当前章节） |

`define` 单独设窄，是因为它只是查一个词，塞整课正文只会拖慢响应。
实测：define 的 prompt 从 ~3700 字符降到 **~1800 字符**，而定义质量没有下降（仍会引用课内数字）。

---

## 三、网页版桥接的额外包装（配置页看不到）

走 `browser` 线路时 chat.deepseek.com 没有 system role，`messagesToSinglePrompt()` 把 system 内容降级塞进用户消息：

```
以下是我的提问要求，请严格遵守：
{ systemPrompt 原文 }

======

{ 完整上下文 + 问题 }

---

（我之前的追问）
{ 上一轮回答 }
```

> ⚠️ **两条线路的约束力不同**：`api` / `local` 走真正的 system role，约束强；
> `browser` 是"用户消息里的一段要求"，约束弱。同一套提示词在两条线路上表现会有差异。

---

## 四、三种模式的完整拼装

| 模式 | system | 上下文范围 | 问题措辞 |
|---|---|---|---|
| 解释这个词 | `systemPrompt` | **当前章节** | `prompts.define` |
| 解释这段 | `systemPrompt` | 整课全文 | `prompts.explain` |
| 自由追问 | `systemPrompt` | 整课全文 | 用户输入；为空时用「请解释或回应我上面引用的内容。」 |

三种模式各约 1.8k / 3.9k / 3.7k 字符。

---

## 五、改提示词时的注意事项

1. **`systemPrompt` 是共用的**，改它等于同时改三种模式。要针对某个模式调整，
   请改对应的 `prompts.*`，而不是往 system 里加偏向性指令。
2. **冲突时以更具体的为准** —— system 末尾已声明这条仲裁规则，往任一 `prompts.*`
   里写具体要求都会覆盖 system 的通用说法。
3. **改完不用重启**：在 `/config` 页保存即可热生效；直接改 `ask.config.json` 文件则需要重启。
4. **`prompts.define` 别要求"不要出现这个词"** —— 那会让模型写出缺主语的句子。
   要禁止的是「这个词是指…」这类铺垫句式，不是词本身。
